# Install

Download and unzip JDK:
```
http://jdk.java.net/
```

# Notes
Not required to add ; (added automatically)

# Add to path
If you need global access.
OS dependent how easy this is...

## MacOS
Copy the jdk folder entirely to:
```
cd /Library/Java/JavaVirtualMachines
```
Then (assuming it's in downloads)
```
mv ~/Downloads/jdk-14.jdk .
```

```
sudo !!
```


# JShell

List typed snippets:
```
/list
```

Dump to file:
```
/save
```
